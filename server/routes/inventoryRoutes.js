import { Router } from 'express'
import { allowRoles, authenticate } from '../middleware/auth.js'
import { createAdjustment, createItem, createPurchase, createUsage, history, listItems, removeItem, updateItem } from '../controllers/inventoryController.js'
const router=Router();router.use(authenticate);router.get('/items',allowRoles('ADMIN','CASHIER'),listItems);router.post('/items',allowRoles('ADMIN'),createItem);router.patch('/items/:id',allowRoles('ADMIN'),updateItem);router.delete('/items/:id',allowRoles('ADMIN'),removeItem);router.get('/items/:id/history',allowRoles('ADMIN'),history);router.post('/purchases',allowRoles('ADMIN','CASHIER'),createPurchase);router.post('/usage',allowRoles('ADMIN'),createUsage);router.post('/adjustments',allowRoles('ADMIN'),createAdjustment);export default router
