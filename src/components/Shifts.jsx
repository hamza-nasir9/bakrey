import { useEffect,useState } from 'react'
import { AlertCircle, ArrowLeft, LockKeyhole, Play, Search, X } from 'lucide-react'
import api from '../lib/api.js'
import './shifts.css'
const money=n=>new Intl.NumberFormat('en-PK',{style:'currency',currency:'PKR',maximumFractionDigits:0}).format(n||0)
export default function Shifts({user,logout}){const [current,setCurrent]=useState(null),[shifts,setShifts]=useState([]),[error,setError]=useState(''),[form,setForm]=useState(null),[close,setClose]=useState(false),[search,setSearch]=useState('');const load=async()=>{try{const currentResponse=await api.get('/shifts/current');setCurrent(currentResponse.data.shift);if(user.role==='ADMIN'){const list=await api.get('/shifts',{params:{search}});setShifts(list.data.shifts)}setError('')}catch(e){setError(e.response?.data?.message||'Unable to load shifts.')}};useEffect(()=>{load()},[search]);const start=async e=>{e.preventDefault();try{await api.post('/shifts/start',form);setForm(null);load()}catch(e){setError(e.response?.data?.message||'Could not start shift.')}};const end=async e=>{e.preventDefault();try{await api.post('/shifts/close',close);setClose(false);load()}catch(e){setError(e.response?.data?.message||'Could not close shift.')}};const reopen=async id=>{try{await api.post(`/shifts/${id}/reopen`);load()}catch(e){setError(e.response?.data?.message||'Could not reopen shift.')}};return <div className="shift-page"><header><button onClick={()=>history.back()}><ArrowLeft size={18}/>Dashboard</button><div><b>AL KAUSAR BAKREY</b><span>Shift Management</span></div><button onClick={logout}>Sign out</button></header><main><div className="shift-title"><div><p>SHIFT MANAGEMENT</p><h1>{user.role==='ADMIN'?'Cashier shifts':'My shift'}</h1><span>Open, close, and reconcile shift cash collections.</span></div>{!current?<button className="shift-primary" onClick={()=>setForm({shiftNumber:'1',openingCash:'0',notes:''})}><Play size={16}/>Start Shift</button>:<button className="close-shift" onClick={()=>setClose({closingCash:'',notes:''})}><LockKeyhole size={16}/>End Shift</button>}</div>{error&&<div className="shift-error"><AlertCircle size={18}/>{error}<button onClick={()=>setError('')}><X size={15}/></button></div>}<section className="current-card"><p>ACTIVE SHIFT</p>{current?<><h2>{current.shiftId} · Shift {current.shiftNumber}</h2><div><span>Opening Cash <b>{money(current.openingCash)}</b></span><span>Started <b>{new Date(current.shiftStartTime).toLocaleString()}</b></span><span>Status <b>Open</b></span></div></>:<p>No active shift. Start a shift before creating orders or receiving payments.</p>}</section>{user.role==='ADMIN'&&<><div className="shift-search"><Search size={17}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search shift or cashier"/></div><section className="shift-table"><table><thead><tr><th>Shift ID</th><th>Cashier</th><th>Shift</th><th>Opening</th><th>Collection</th><th>Closing</th><th>Difference</th><th>Status</th><th>Action</th></tr></thead><tbody>{shifts.map(s=><tr key={s._id}><td><b>{s.shiftId}</b></td><td>{s.cashier?.name}</td><td>Shift {s.shiftNumber}</td><td>{money(s.openingCash)}</td><td>{money(s.systemCashCollection)}</td><td>{s.closingCash===undefined?'—':money(s.closingCash)}</td><td>{money(s.cashDifference)}</td><td><span className={s.status==='Open'?'open':'closed'}>{s.status}</span></td><td>{s.status==='Closed'&&<button onClick={()=>reopen(s._id)}>Reopen</button>}</td></tr>)}</tbody></table></section></>}
{form&&<Modal title="Start Shift" close={()=>setForm(null)}><form onSubmit={start} className="shift-form"><label>Shift Number<select value={form.shiftNumber} onChange={e=>setForm({...form,shiftNumber:e.target.value})}>{[1,2,3,4].map(x=><option key={x}>{x}</option>)}</select></label><label>Opening Cash<input required min="0" type="number" value={form.openingCash} onChange={e=>setForm({...form,openingCash:e.target.value})}/></label><label>Notes<textarea value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/></label><button className="shift-primary">Start Shift</button></form></Modal>}{close&&<Modal title="End Shift" close={()=>setClose(false)}><form onSubmit={end} className="shift-form"><label>Closing Cash<input required min="0" type="number" value={close.closingCash} onChange={e=>setClose({...close,closingCash:e.target.value})}/></label><label>Remarks<textarea value={close.notes} onChange={e=>setClose({...close,notes:e.target.value})}/></label><button className="close-shift">Close & Reconcile</button></form></Modal>}</main></div>}
function Modal({title,close,children}){return <div className="shift-back" onMouseDown={close}><section className="shift-modal" onMouseDown={e=>e.stopPropagation()}><div><h2>{title}</h2><button onClick={close}><X/></button></div>{children}</section></div>}
