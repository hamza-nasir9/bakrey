import { useEffect, useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { AlertCircle, ArrowDownRight, ArrowUpRight, Bell, Boxes, CalendarDays, ChevronDown, ClipboardList, HandCoins, LayoutDashboard, Menu, PackagePlus, ReceiptText, RefreshCw, Truck, Users, Wallet } from 'lucide-react'
import api from '../lib/api.js'
import './dashboard.css'
const money = value => new Intl.NumberFormat('en-PK', { style:'currency', currency:'PKR', maximumFractionDigits:0 }).format(value || 0)
const numbers = value => new Intl.NumberFormat('en-PK').format(value || 0)
const nav = [['Dashboard',LayoutDashboard,'/dashboard'],['Inventory',Boxes,'/inventory'],['Orders',ClipboardList,'/orders'],['Payments',Wallet,'/payments'],['Customers',Users,'/customers'],['Suppliers',Truck,'/suppliers'],['Employees',Users,'/employees'],['Attendance',Users,'/attendance'],['Salary',HandCoins,'/salaries'],['Expenses',ReceiptText,'/expenses'],['Shifts',CalendarDays,'/shifts'],['Daily Closing',ClipboardList,'/daily-closing'],['Reports',ReceiptText,'/reports'],['User Management',Users,'/users'],['System Center',Bell,'/system']]
const definitions = [['Total Sales','totalSales',Wallet],['Total Revenue','totalRevenue',ArrowUpRight],['Total Expenses','totalExpenses',ReceiptText],['Total Purchase','totalPurchase',PackagePlus],['Total Salary','totalSalary',HandCoins],['Net Profit','netProfit',ArrowUpRight],['Pending Payments','pendingPayments',Wallet],['Total Orders','totalOrders',ClipboardList],['Completed Orders','completedOrders',ClipboardList],['Pending Orders','pendingOrders',AlertCircle],['Total Employees','totalEmployees',Users],['Total Suppliers','totalSuppliers',Truck],['Current Inventory','currentInventory',Boxes],['Low Stock Items','lowStockItems',AlertCircle]]
function Chart({ title, data, color }) { const max = Math.max(...data.map(x=>x.value), 1); const points = data.length === 1 ? '0,145 300,145' : data.map((x,i)=>`${(i/(data.length-1))*300},${145-(x.value/max)*120}`).join(' '); return <article className="dash-panel chart-panel"><div><p className="dash-kicker">ANALYTICS</p><h3>{title}</h3></div><div className="svg-wrap"><svg viewBox="0 0 300 155" preserveAspectRatio="none"><line x1="0" y1="145" x2="300" y2="145"/><line x1="0" y1="95" x2="300" y2="95"/><line x1="0" y1="45" x2="300" y2="45"/><polyline points={points} fill="none" stroke={color} strokeWidth="3"/></svg><div className="chart-labels">{data.map(x=><span key={x.date}>{x.label}</span>)}</div></div></article> }
export default function Dashboard({ user, logout }) { const navigate=useNavigate(); const [period,setPeriod]=useState('daily'), [data,setData]=useState(null), [loading,setLoading]=useState(true), [error,setError]=useState(''), [menu,setMenu]=useState(false); const load=async()=>{setLoading(true);setError('');try{const response=await api.get(`/dashboard/summary?period=${period}`);setData(response.data)}catch(e){setError(e.response?.data?.message || 'Unable to load dashboard data. Check the API connection.')}finally{setLoading(false)}}; useEffect(()=>{load()},[period]); if(user.role!=='ADMIN') return <div className="access-denied"><AlertCircle/><h2>Dashboard access is restricted</h2><p>Cashiers can access only their current shift, new orders, payment receipt, and shift closing.</p><button onClick={logout}>Sign out</button></div>; return <div className="dashboard-shell"><aside className={menu?'show':''}><div className="dash-brand"><b>AK</b><span>AL KAUSAR<small>BAKREY</small></span></div><p>BUSINESS MANAGEMENT</p>{nav.map(([name,Icon,path])=><button onClick={()=>navigate(path)} className={name==='Dashboard'?'active':''} key={name}><Icon size={17}/>{name}</button>)}<div className="dash-user"><b>{user.name?.slice(0,1)}</b><span><strong>{user.name}</strong><small>Owner / Admin</small></span><button onClick={logout}>Sign out</button></div></aside><main><header><button className="dash-menu" onClick={()=>setMenu(!menu)}><Menu/></button><div><span>AL KAUSAR BAKREY</span><b>/ Dashboard</b></div><section><button><CalendarDays size={15}/>{new Intl.DateTimeFormat('en-PK',{dateStyle:'medium'}).format(new Date())}</button><button><Bell size={18}/></button><i>{user.name?.slice(0,1)}</i></section></header><div className="dash-content"><div className="dash-title"><div><p className="dash-kicker">OVERVIEW</p><h1>Business at a glance</h1><span>Live business results from your MongoDB records.</span></div><div className="periods">{['daily','weekly','monthly'].map(x=><button onClick={()=>setPeriod(x)} className={period===x?'chosen':''} key={x}>{x}</button>)}<button onClick={load} aria-label="Refresh dashboard"><RefreshCw size={16}/></button></div></div>{error?<div className="dash-error"><AlertCircle/><div><b>Dashboard data could not be loaded</b><p>{error}</p></div><button onClick={load}>Try again</button></div>:loading?<div className="dash-loading">Loading live dashboard data…</div>:<><div className="dash-cards">{definitions.map(([label,key,Icon])=><article key={key}><div><Icon size={17}/></div><p>{label}</p><h2>{key.includes('Orders')||key.includes('Employees')||key.includes('Suppliers')||key.includes('Items')||key==='currentInventory'?numbers(data.cards[key]):money(data.cards[key])}</h2><small>{key==='lowStockItems'?'Items at or below their stock threshold':key==='currentInventory'?'Total unit quantity on hand':'Calculated from saved database records'}</small></article>)}</div><div className="dash-charts"><Chart title="Revenue" data={data.charts.revenue} color="#188044"/><Chart title="Expenses" data={data.charts.expenses} color="#c9781b"/><Chart title="Net Profit" data={data.charts.profit} color="#7053b7"/><Chart title="Orders" data={data.charts.orders} color="#3e77b5"/></div><div className="dash-bottom"><article className="dash-panel activities"><div><p className="dash-kicker">LIVE DATABASE LOG</p><h3>Recent activities</h3></div>{data.recentActivities.length?data.recentActivities.map(a=><div className="dash-activity" key={a.id}><b><ReceiptText size={15}/></b><span><strong>{a.action}</strong><small>{a.description}</small></span><span>{a.amount>0&&<strong>{money(a.amount)}</strong>}<small>{new Intl.DateTimeFormat('en-PK',{dateStyle:'medium',timeStyle:'short'}).format(new Date(a.occurredAt))}</small></span></div>):<p className="blank">No activities have been recorded yet.</p>}</article><article className="dash-panel lowstock"><div><p className="dash-kicker">INVENTORY ALERT</p><h3>Low stock status</h3></div><div className="stock-alert"><AlertCircle/><span><b>{numbers(data.cards.lowStockItems)} low stock items</b><small>Inventory alerts update automatically from stock quantities and thresholds.</small></span></div><p>Current inventory <b>{numbers(data.cards.currentInventory)} units</b></p><p>Pending customer payments <b>{money(data.cards.pendingPayments)}</b></p></article></div></>}</div></main></div> }
